import React from "react";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { ChevronDown, Search, User, Heart, ShoppingCart, Phone } from "lucide-react";

const Navbar = () => {
    return (
        <div></div>
    );
};

export default Navbar;

